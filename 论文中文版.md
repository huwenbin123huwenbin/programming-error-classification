# 机器学习与大语言模型在竞赛编程错误模式识别中的比较分析

## Comparative Analysis of Machine Learning and Large Language Models for Programming Error Pattern Recognition in Competitive Programming

> 这是英文论文 `paper.tex` 的中文翻译版，仅供快速阅读参考。正式引用请以英文版为准。
> 
> 📅 最后更新：2026-06-20 | 数据集版本：13,360 样本（纯真实 Codeforces 数据）

---

## 摘要

**背景**：全球入门编程课程的挂科率和辍学率长期维持在 30%–40%，关键瓶颈在于学生难以有效诊断编程错误。传统机器学习（ML）分类器在自动化错误分类方面已有进展，但大语言模型（LLM）为该任务提供了全新范式，其有效性尚未被系统验证。

**目标**：本文首次系统比较传统 ML 分类器与 LLM 方法，仅使用提交元数据（无需源代码）进行编程错误类型分类，填补“相同数据集、相同评估协议”直接对比的研究空白。

**方法**：从 Codeforces 收集 13,360 个真实错误提交，涵盖 5 种错误类型：Wrong Answer (76.6%)、Time Limit Exceeded (9.6%)、Compilation Error (7.3%)、Runtime Error (4.9%)、Memory Limit Exceeded (1.5%)。三种 ML 分类器（Random Forest、Gradient Boosting、Logistic Regression）与三种 LLM 配置（DeepSeek-V3 zero-shot、DeepSeek-V3 few-shot 5-shot、Qwen2.5:3B zero-shot）在分层 80/20 训练-测试分割和 5 折交叉验证下评估，使用 McNemar 检验（α = 0.05）判断显著性，并通过系统删除特征进行消融研究。

**结果**：Gradient Boosting 达到最高测试准确率 95.02%（95% CI [94.2%, 95.9%]），Random Forest 为 94.24%（95% CI [93.3%, 95.0%]）。两者均显著优于 Logistic Regression (73.91%, p < 0.001)。在大语言模型方面，DeepSeek-V3 zero-shot 达到 78.29%（0% 无效预测），远优于 few-shot (17.37%, 31.3% 无效预测)；Qwen2.5:3B 仅 11.34%。执行时间是最强预测因子：删除后准确率下降 8.8 个百分点 (p < 0.001)。Runtime Error 仍是最难类别（F1 ≈ 0.52）。Prompt 设计对 LLM 性能影响巨大（zero-shot 比 few-shot 高 60.9 pp）。

**贡献**：(1) 首个基于元数据的 ML-LLM 错误分类对比；(2) 包含功效分析、效应量和显著性检验的全面统计分析；(3) 执行时间主导性发现（消融验证 8.8 pp 下降）；(4) 面向六种教育场景的方法选择指南；(5) 公开数据集、预处理流程与实验代码，便于复现。

**关键词**：编程教育、错误分类、机器学习、大语言模型、竞赛编程、元数据分析

---

## 1. 引言

### 1.1 问题背景

编程教育的核心挑战是学生难以有效诊断代码错误。全球入门编程课程的挂科/辍学率长期维持在 30%–40%。自动化错误诊断工具可分为两类：基于提交元数据（执行时间、内存消耗等）的传统机器学习分类器，以及利用代码/元数据理解能力的大语言模型。

### 1.2 研究空白

此前研究大多只评估 ML 或只评估 LLM，缺乏在相同数据集、相同评估协议下的直接比较。本文填补这一空白，重点回答：**仅使用元数据时，ML 与 LLM 的相对表现如何？**

### 1.3 研究问题

- **RQ1**: 仅基于元数据，ML 分类器能否有效识别编程错误类型？
- **RQ2**: 哪些特征对错误分类贡献最大？
- **RQ3**: 与 ML 相比，LLM 方法（DeepSeek-V3 zero-shot/few-shot、Qwen2.5:3B）的表现如何？
- **RQ4**: 不同教育场景下应如何选择 ML 或 LLM？
- **RQ5**: 数据量、模型复杂度与部署约束如何影响选择？

### 1.4 论文结构

第 2 节 → 相关工作；第 3 节 → 方法论；第 4 节 → 结果；第 5 节 → 讨论；第 6 节 → 结论。

---

## 2. 相关工作

### 2.1 编程错误分析

错误分类研究从早期的语法/语义/逻辑三分法，发展到更细粒度的运行时分类。近年趋势是利用执行时间、内存消耗等运行时特征辅助错误诊断。

### 2.2 计算机教育中的机器学习

ML 在编程教育中的应用包括学生表现预测、代码自动评分、错误类型分类。典型方法包括决策树、随机森林、梯度提升，特征涵盖代码复杂度、提交历史、运行时指标。

### 2.3 编程领域的大语言模型

LLM（GPT-4、CodeLlama、DeepSeek-Coder 等）在代码生成和理解上表现突出，但基于元数据（而非源代码）进行错误分类的系统性评估仍较缺乏。本文在统一协议下对比三种 ML 方法与三种 LLM 配置（DeepSeek-V3 zero-shot/few-shot、Qwen2.5:3B）。

---

## 3. 方法论

### 3.1 数据集收集

- **来源**：Codeforces 公共 REST API
- **收集过程**：选取多个比赛题目，获取所有非 Accepted 的错误提交，过滤非 C/C++ 语言和非标准 verdict，合并去重
- **最终数据集**：**13,360** 个真实错误样本，来自纯真实 Codeforces 提交（无合成数据）
- **训练/测试划分**：分层 80/20 → **10,688** 训练 + **2,672** 测试

**整体类别分布**：

| 错误类型 | 数量 | 占比 |
|---------|------|------|
| Wrong Answer (WA) | 10,234 | 76.6% |
| Time Limit Exceeded (TLE) | 1,288 | 9.6% |
| Compilation Error (CE) | 980 | 7.3% |
| Runtime Error (RE) | 656 | 4.9% |
| Memory Limit Exceeded (MLE) | 202 | 1.5% |

**测试集类别分布**：

| 错误类型 | 测试集数量 |
|---------|------------|
| WA | 2,047 |
| TLE | 258 |
| CE | 196 |
| RE | 131 |
| MLE | 40 |

### 3.2 特征工程

最终模型使用 **7 个元数据特征**（无需源代码）：

| 特征 | 描述 | 类型 |
|------|------|------|
| `time_consumed_ms` | 执行时间（毫秒） | 连续 |
| `memory_kb` | 内存消耗（KB） | 连续 |
| `problem_rating` | 题目难度评分 | 连续 |
| `language_encoded` | 编程语言编码 | 类别 |
| `problem_type` | 题目类型（A/B/C/D 等） | 类别 |
| `hour` | 提交时间（小时） | 类别 |
| `user_success_rate` | 用户历史通过率 | 连续 |

### 3.3 样本量论证

- 5 分类、预期准确率 85%、α=0.05、功效 80% 所需最小样本量：约 **889**
- 实际样本量：**13,360**，超出阈值约 **22 倍**
- Cohen's h（GB vs LR）= 0.14，Bootstrap 95% CI 为 GB [94.2%, 95.9%]，确认样本充分

### 3.4 实验模型

**ML 模型**：
- **Random Forest**：200 棵树，最大深度 15
- **Gradient Boosting**：200 棵树，最大深度 5
- **Logistic Regression**：最大迭代 1000 次

**LLM 模型**：
- **DeepSeek-V3 zero-shot**：无示例，直接给出 metadata 字段和分类要求
- **DeepSeek-V3 few-shot（5-shot）**：每类 1 个示例，temperature=0，max_tokens=10
- **Qwen2.5:3B zero-shot**：本地部署 via Ollama，无示例

### 3.5 评估协议

- **训练/测试分割**：分层 80/20（10,688 / 2,672）
- **交叉验证**：5 折分层 CV
- **统计检验**：McNemar 检验（α = 0.05）
- **消融研究**：对 Gradient Boosting 逐一删除特征，测量准确率下降

---

## 4. 结果

### 4.1 传统 ML 性能

| 模型 | 测试准确率 | 5 折 CV (Mean±SD) | 宏 F1 |
|------|-----------|------------------|-------|
| **Gradient Boosting** | **95.02%** | 95.13% ± 0.39 | **0.8711** |
| Random Forest | 94.24% | 94.39% ± 0.65 | 0.8543 |
| Logistic Regression | 73.91% | 74.31% ± 3.26 | 0.6738 |

**Random Forest 各类别 F1**：

| 类别 | F1 | 说明 |
|------|-----|------|
| TLE | 0.98 | 超时特征极其明显 |
| MLE | 0.91 | 高内存消耗形成强信号 |
| WA | 0.97 | 主流类别，表现稳定 |
| CE | 0.89 | 执行时间为 0，易区分 |
| RE | 0.52 | **最难**，异质性强 |

**Gradient Boosting 各类别 F1**：TLE 0.99，WA 0.97，MLE 0.96，CE 0.89，RE 0.53。

### 4.2 特征重要性

基于 Random Forest 的 Gini 重要性：

| 排名 | 特征 | 重要性 |
|------|------|--------|
| 1 | `time_consumed_ms` | **42.5%** |
| 2 | `memory_kb` | 24.2% |
| 3 | `user_success_rate` | 19.4% |
| 4 | `problem_rating` | 5.7% |
| 5 | `language_encoded` | 5.0% |
| 6 | `problem_type` | 3.2% |
| 7 | `hour` | 0.0% |

### 4.3 消融分析（Gradient Boosting）

| 删除特征 | 准确率 | 下降幅度 | 说明 |
|---------|--------|---------|------|
| 无（完整模型） | 95.02% | — | 基线 |
| `time_consumed_ms` | 86.19% | **↓ 8.8 pp** | 绝对主导特征 |
| `user_success_rate` | 91.39% | ↓ 3.6 pp | 用户历史贡献显著 |
| `memory_kb` | 93.64% | ↓ 1.4 pp | 内存次重要 |
| `language_encoded` | 94.76% | ↓ 0.3 pp | 语言影响有限 |
| `problem_rating` | 94.95% | ↓ 0.1 pp | 题目难度影响极小 |
| `problem_type` | 94.87% | ↓ 0.1 pp | 题目类型影响极小 |
| `hour` | 95.02% | 0.0 pp | 无贡献 |

### 4.4 LLM 性能对比

| 模型 | 测试准确率 | 宏 F1 | 成本/千次 | 延迟 |
|------|-----------|-------|----------|------|
| **Gradient Boosting (ML)** | **95.02%** | **0.8711** | $0.01 | ~2 ms |
| Random Forest (ML) | 94.24% | 0.8543 | $0.01 | ~2 ms |
| Logistic Regression (ML) | 73.91% | 0.6738 | $0.01 | ~1 ms |
| DeepSeek-V3 zero-shot | 78.29% | 0.3467 | $0.08–0.12 | ~1 s |
| DeepSeek-V3 5-shot | 17.37% | 0.1724 | $0.10–0.15 | ~3 s |
| Qwen2.5:3B zero-shot | 11.34% | 0.0603 | $0.00 | ~10 s |
| CodeT5+_GRU（文献） | 91.84% | 0.8240 | GPU only | — |
| CodeBERT Fine-Tuned（文献） | 86.1% | N/A | $2+GPU | — |
| LLaMA-3 8B Zero-Shot（文献） | 82.0% | N/A | GPU only | — |

DeepSeek-V3 zero-shot 达到 78.29%（0% 无效预测），远优于 few-shot (17.37%, 31.3% 无效预测)，说明简单直接的 prompt 设计远优于 few-shot 示例。Qwen2.5:3B 仅 11.34%，表明小型本地 LLM（< 4B 参数）完全不适合此任务。

**DeepSeek 各类别 F1（zero-shot）**：CE=0.86, MLE=0.97, RE=0.00, TLE=0.04, WA=0.88。RE 和 TLE F1 极低，说明 zero-shot 在细粒度错误分类上仍有不足。

**DeepSeek 5-shot 各类别 F1**：

| 类别 | F1 |
|------|-----|
| MLE | 0.4025 |
| CE | 0.2078 |
| WA | 0.1246 |
| TLE | 0.1029 |
| RE | 0.0241 |

### 4.5 McNemar 检验（全量测试集 n=2,672）

| 对比 | χ² | p 值 | 显著？ |
|------|-----|------|--------|
| GB vs RF | 5.33 | 0.021 | ✅ 显著 |
| GB vs LR | > 1,000 | < 0.001 | ✅ 显著 |
| RF vs LR | > 1,000 | < 0.001 | ✅ 显著 |
| GB vs DeepSeek 0-shot | 370.42 | < 0.001 | ✅ 显著 |
| GB vs DeepSeek 5-shot | 2,200.16 | < 0.001 | ✅ 显著 |
| GB vs Qwen 2.5:3B | 2,222.08 | < 0.001 | ✅ 显著 |
| RF vs DeepSeek 5-shot | 2,169.38 | < 0.001 | ✅ 显著 |
| LR vs DeepSeek 5-shot | 较大 | < 0.001 | ✅ 显著 |

GB 与 RF 虽统计显著，但实际差距仅 0.78 pp，选择应基于部署约束而非准确率本身。

---

## 5. 讨论

### 5.1 关键发现

**发现 1：Prompt 设计显著影响 LLM 性能**

DeepSeek zero-shot (78.29%) 远优于 few-shot (17.37%)，差距达 60.9 pp。这表明简单直接的 zero-shot prompt 在元数据分类任务上更有效，few-shot 示例反而可能干扰模型判断。

**发现 2：传统 ML 大幅优于所有 LLM 配置**

GB 95.02% vs DeepSeek zero-shot 78.29%，差距约 16.7 pp。即使性能最好的 LLM 配置也显著低于 ML（McNemar χ²=370.42, p<0.001）。

**发现 3：小型本地 LLM 完全不具竞争力**

Qwen2.5:3B 仅 11.34%，说明 3B 参数级别的本地 LLM 完全无法处理元数据分类任务。

**发现 4：执行时间是绝对主导特征**
RF/GB 对 RE 的 F1 仅约 0.52–0.53，因其异质性（段错误、除零、断言失败等）无法仅靠元数据区分。

**发现 5：泛化性验证——跨用户与跨难度**
Leave-One-User-Out CV（130位用户，逐一留出训练）平均准确率 **82.5%**（95% CI: 78.6–86.4%），证明模型能泛化到未见用户。按题目难度（problem_rating）分层的 Cross-Difficulty 分析显示：Easy→Easy 90.1–93.6%，Medium→Medium 87.5%，Easy→Hard 71.3%，Medium→Easy 85.9%。最难场景（Easy→Hard）71.3% 仍远高于 DeepSeek 基线，说明模型在难度迁移上有一定鲁棒性但非完美。

**发现 6：按用户技能分层的性能一致**
以用户 Codeforces rating 作为技能代理，将测试集分为 Novice（<1400，n=1,853）、Intermediate（1400–1999，n=522）、Advanced（≥2000，n=288）三组。GB 模型在 Novice 组准确率最高（96.0%），Advanced 组最低（88.5%），反直觉但合理：Novice 错误更典型（WA/TLE/CE），Advanced 错误更多样且微妙。即便 Advanced 组也远超 DeepSeek 17.37% 基线。

**发现 7：混合 ML+LLM 方案潜力巨大**
ML 负责快速、低成本分类，LLM 在需要时生成自然语言解释，兼顾准确率与教学价值。

### 5.2 方法选择指南

| 场景 | 推荐方法 | 理由 |
|------|---------|------|
| 高频实时 IDE 反馈 | Random Forest | 延迟约 2 ms，$0.01/千次 |
| 自动评分后台 | Gradient Boosting | 准确率最高（95.02%），CV 稳定 |
| 个性化辅导 | 混合 ML + LLM | ML 分类 + LLM 解释 |
| 隐私敏感场景 | Random Forest | 本地运行，无需 API |
| 科研/基线对比 | Logistic Regression | 可解释性强，统计基线 |
| 低资源课堂 | Random Forest | CPU 即可，训练快、成本低 |

### 5.3 误差分析

**Random Forest 关键误分类**：
- RE → WA：65 例，主要因为 RE 内部异质性强
- WA → RE：31 例，WA 与部分 RE 的元数据特征重叠
- CE → WA：9 例，CE 通常执行时间为 0，少数例外

**DeepSeek 关键失败**：
- 对 WA 的 recall 极低：仅正确预测 95/2,047 个 WA
- 大量无效预测（835 个），提示词未能引导模型输出标准 verdict
- Pilot 结果（81.64%）在全量分布下失效，说明小样本试点存在严重代表性风险

### 5.5 泛化性实证：跨用户与跨难度分析

为验证模型是否过拟合特定用户或题目难度，我们设计了两项补充实验。

**实验一：Leave-One-User-Out CV**
从测试集中选取提交数 ≥5 的 130 位用户，每位用户依次作为测试集，其余用户作为训练集，训练 GB 模型并评估。130 次实验的平均准确率为 **82.5%**（95% CI: 78.6–86.4%）。91/130 位用户的准确率 ≥80%，且用户样本量与准确率无显著相关（r = -0.18），说明模型能学到跨用户的通用错误模式。

**实验二：Cross-Difficulty 泛化**
按 problem_rating 四分位数将题目分为 Easy（<1400）、Medium（1400–1799）、Hard（≥1800）三组，在 Easy 上训练、在 Medium/Hard 上测试，以此类推。结果：
- Easy → Easy：90.1–93.6%
- Medium → Medium：87.5%
- Easy → Hard：**71.3%**（最难迁移场景）
- Medium → Easy：85.9%

即便最难迁移场景（71.3%）也远高于 DeepSeek 17.37% 基线，说明模型的 metadata 信号具有跨难度鲁棒性，尽管在高难度题目上性能有所下降。

### 5.6 学生代理验证：跨技能等级泛化

教育部署的核心关切是：我们的发现能否推广到初学者？Codeforces 用户涵盖广泛技能水平，我们以用户 rating 作为编程专业度的代理，将测试集按 rating 分为三组：

| 技能等级 | Rating 范围 | 测试样本 |
|---------|-----------|--------|
| Novice（初学者） | < 1,400 | 1,853 |
| Intermediate（中级） | 1,400–1,999 | 522 |
| Advanced（高级） | ≥ 2,000 | 288 |

表 6 报告了 GB 模型在各组的性能。反直觉的结果是：模型在 Novice 组表现最好（96.0%），Advanced 组最差（88.5%）。这是因为 Novice 提交的错误更典型（WA/TLE/CE），而 Advanced 用户的错误更多样且微妙，仅凭 metadata 更难分类。即便如此，Advanced 组的准确率（88.5%）仍远超 DeepSeek few-shot 基线（17.37%）。

**表 6：Gradient Boosting 按用户技能等级的性能（测试集，作为学生 vs. 专家人群的代理）**

| 技能等级 | 测试样本 | 准确率（95% CI） | Macro F1 |
|---------|---------|----------------|----------|
| Novice（<1400） | 1,853 | 96.0% [95.1, 96.9] | 0.88 |
| Intermediate（1400–1999） | 522 | 95.2% [93.3, 96.8] | 0.86 |
| Advanced（≥2000） | 288 | 88.5% [84.8, 91.9] | 0.86 |
| 整体 | 2,672 | 95.0% [94.2, 95.9] | 0.87 |

我们强调这是**代理验证**而非对 CS1 学生人群的直接验证。Codeforces Novice 用户仍是自选择的竞赛编程参与者，可能与课堂学生存在差异。然而，模型在最低 rating 用户上表现最强，这强烈暗示该方法能推广到经验较少的人群，包括入门 CS 学生。在真实课堂数据集上的后续研究仍是重要的未来工作。

---

### 5.7 对计算教育研究的更广泛启示

我们的发现对计算教育研究和实践有几个重要启示。首先，仅使用 metadata 的分类（95.02% 准确率，无需源代码访问）表明**提交日志本身**就构成了错误模式识别的充足信号，为隐私保护教育分析开辟了新可能。

其次，传统 ML 相比 LLM 方法具有显著成本优势。一个学期部署（500 名学生，约 5,000 次提交）使用 Random Forest 成本约 $0.50，而 DeepSeek zero-shot API 约 $0.40–0.60、few-shot 约 $0.50–0.75。Qwen2.5:3B 本地部署成本为 $0.00，但准确率仅 11.34%，在实际应用中不可接受。

第三，DeepSeek few-shot 在小样本 pilot（n=207，81.64%）与全量测试集（n=2,672，17.37%）之间的惊人差距，为社区提供了重要的方法论警示。基于小样本、可能不具代表性的 pilot 评估会产生严重误导性结果。

---

### 5.8 局限性与泛化性

三个关键局限：（1）**人群泛化性**——尽管我们的跨技能等级代理分析（第 5.6 节）显示模型在 rating <1400 用户上表现最好，但这些仍是自选择的竞赛编程参与者，可能与课堂 CS1 学生存在差异；（2）**LLM 对比范围**——虽然扩展至三种配置（DeepSeek-V3 zero-shot/few-shot、Qwen2.5:3B），但未测试 GPT-4、Claude、LLaMA-3 等主流大模型；（3）**特征集**——仅使用了 7 个 metadata 特征，源代码特征可能提升 RE 分类（F1 = 0.52）。

尽管如此，两项泛化性实验（第 5.5 节）和跨技能等级验证（第 5.6 节）提供了实质性证据，表明 GB 模型的优越性能不是训练-测试划分的人为产物，而是能跨用户、跨难度、跨技能等级泛化的真实信号。

---

## 6. 结论

本文首次系统比较了传统 ML 与 LLM 在仅使用提交元数据的编程错误分类任务上的表现。基于 13,360 个真实 Codeforces 错误提交的 4 种实验条件（3 个 ML + 1 个 LLM），得出以下结论：

1. **Prompt 设计对 LLM 性能影响巨大**：DeepSeek-V3 zero-shot (78.29%) 远优于 few-shot (17.37%)，差距 60.9 pp。

2. **传统 ML（GB 95.02%）显著优于所有 LLM 配置**：即便 DeepSeek zero-shot (78.29%) 也低于 GB，差距 16.7 pp（McNemar χ²=370.42, p<0.001）。

3. **小型本地 LLM 不具竞争力**：Qwen2.5:3B 仅 11.34%，说明 < 4B 参数模型无法胜任此任务。
2. **执行时间是最强预测因子**，删除导致 8.8 pp 准确率下降。
3. **RF 与 GB 差异统计显著但实践差距小（0.78 pp）**，部署约束是选择关键。
4. **ML 在成本、延迟、隐私方面具有显著优势**（成本约为 LLM 的 10–15 分之一，延迟约 2 ms vs 3 s）。
5. **RE 仍是最难类别**（F1 ≈ 0.52），需要代码级特征才能进一步突破。
6. **混合 ML+LLM 方案最具教学潜力**：ML 快速分类，LLM 生成解释。
7. **学生代理验证显示模型在初学者代理人群上表现最佳**（96.0%），为推广到 CS1 课堂提供了初步证据。

**未来方向**：扩展更多大模型（GPT-4、Claude、LLaMA-3）、评估更大本地模型（Qwen2.5:7B/14B）、优化 zero-shot prompt、引入源代码特征、在初学者数据集上验证、开发更细粒度的错误分类体系。

---

---

*📄 英文版：`/Users/mac/Desktop/SCI1/06_论文定稿/paper.tex`*  
*📊 实验结果：`/Users/mac/Desktop/SCI1/06_论文定稿/final_experiments_v2/final_results_v2.json`*  
*📅 最后更新：2026-06-20（已同步至 23 页英文版，含泛化性实验与学生代理验证）*
